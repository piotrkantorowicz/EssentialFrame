name: Build package - EssentialFrame.Cache

on:
  push:
    branches:
      - main

    paths:
      - src/api/EssentialFrame.Cache/EssentialFrame.Cache.csproj

jobs:
  build-and-push-package:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        dotnet-version: [ "7.0.x" ]
    steps:
      - name: Checkout the repository
        uses: actions/checkout@v2
        with:
          token: ${{ secrets.ACTION_GITHUB_TOKEN }}
          fetch-depth: 0

      - name: Setup .NET
        uses: actions/setup-dotnet@v2
        with:
          dotnet-version: ${{ matrix.dotnet-version }}
          source-url: https://nuget.pkg.github.com/piotrkantorowicz/index.json
        env:
          NUGET_AUTH_TOKEN: ${{secrets.ACTION_GITHUB_TOKEN}}

      - name: Restore
        run: dotnet restore src/api/EssentialFrame.Cache/EssentialFrame.Cache.csproj
      - name: Build
        run: dotnet build src/api/EssentialFrame.Cache/EssentialFrame.Cache.csproj --configuration Release --no-restore
